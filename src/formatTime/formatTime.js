'use strict';

/**
 * Implement formatTime function:
 *
 * Function takes a number of minutes and returns string with time in HH:MM
 * format.
 *
 * formatTime(180) === 3:00
 * formatTime(299) === 4:59
 *
 * @param {number} minutes
 *
 * @return {string}
 */
function formatTime(minutes) {
  // write code here
}

module.exports = formatTime;
