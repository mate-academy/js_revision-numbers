'use strict';

/**
 * Implement generateRandomNumber function:
 *
 * Function returns random float or integer number in range from 1 (including)
 * to 10 (not including). In Math - range [1, 10).
 *
 * @return {number}
 */
function generateRandomNumber() {
  // write code here
}

module.exports = generateRandomNumber;
