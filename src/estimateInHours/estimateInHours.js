'use strict';

/**
 * Implement estimateInHours function:
 *
 * John's productivity is 'productivity' (positive natural number) tasks per
 * hour. He has got 'tasks' (non-negative natural number) tasks and has to
 * estimate how many full hours does he need to complete all given tasks.
 *
 * estimateInHours(5, 13) === 3
 *    John can't complete all tasks in 2 hours so he needs 3 hours
 * estimateInHours(5, 100) === 20
 *    John's estimation is 20 full hours to complete all given tasks
 *
 * @param {number} productivity
 * @param {number} tasks
 *
 * @return {number}
 */
function estimateInHours(productivity, tasks) {
  // write code here
}

module.exports = estimateInHours;
