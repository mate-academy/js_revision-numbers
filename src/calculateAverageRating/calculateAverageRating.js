'use strict';

/**
 * Implement calculateAverageRating function:
 *
 * Function takes 5 non-negative natural numbers (countOne, countTwo,
 * countThree, countFour and countFive). Each of numbers represents amount of
 * people rated product with 1, 2, 3, 4 or 5 stars. Function should return
 * average rating of the product.
 *
 * calculateAverageRating(0, 1, 6, 1, 4) should return a number close to 3.6666
 * calculateAverageRating(1, 1, 1, 1, 1) should return 3
 *
 * @param {number} countOne
 * @param {number} countTwo
 * @param {number} countThree
 * @param {number} countFour
 * @param {number} countFive
 *
 * @return {number}
 */
function calculateAverageRating(
  countOne, countTwo, countThree, countFour, countFive) {
  // write code here
}

module.exports = calculateAverageRating;
