'use strict';

/**
 * Implement calculateAverageRating function:
 *
 * Function takes 5 non-negative natural numbers (one, two, three, four and
 * five). Each of numbers represents amount of people rated product with 1, 2,
 * 3, 4 or 5 stars. Function should return average rating of the product.
 *
 *
 *
 * calculateAverageRating(0, 1, 6, 1, 4) should return a number close to 3.6666
 * calculateAverageRating(1, 1, 1, 1, 1) should return 3
 *
 * @param {number} one
 * @param {number} two
 * @param {number} three
 * @param {number} four
 * @param {number} five
 *
 * @return {number}
 */
function calculateAverageRating(one, two, three, four, five) {
  // write code here
}

module.exports = calculateAverageRating;
