'use strict';

/**
 * Implement calculateWidth function:
 *
 * Function takes 2 strings: width in pixels 'width' (e.g. '1200px') and some
 * percentage 'percentage' (e.g. '15%') and returns number which represents
 * amount of pixels according to given width and percentage. Return 0 if at
 * least one of given params doesn't start with number.
 *
 * calculateWidth('1200px', '15%') === 180
 * calculateWidth('100px', '150%') === 150
 * calculateWidth('100px', '%150') === 0
 *
 * @param {string} width
 * @param {string} percentage
 *
 * @return {number}
 */
function calculateWidth(width, percentage) {
  // write code here
}

module.exports = calculateWidth;
