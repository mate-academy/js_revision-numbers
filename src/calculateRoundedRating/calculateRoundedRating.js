'use strict';

/**
 * Implement calculateRoundedRating function:
 *
 * Function takes an exact rating of the product - the float number between 1
 * and 5 including (range `[1, 5]`) and returns the rounded rating to closest
 * integer (1, 2, 3, 4 or 5).
 *
 * calculateRoundedRating(3.66666) === 4
 * calculateRoundedRating(1.2) === 1
 *
 * @param {number} rating
 *
 * @return {number}
 */
function calculateRoundedRating(rating) {
  // write code here
}

module.exports = calculateRoundedRating;
