'use strict';

/**
 * Implement generateRandomInteger function:
 *
 * Function takes positive integer number N and returns random integer number in
 * range from 1 (including) to N (including). In Math - range [1, N].
 *
 * @param {number} N
 *
 * @return {number}
 */
function generateRandomInteger(N) {
  // write code here
}

module.exports = generateRandomInteger;
